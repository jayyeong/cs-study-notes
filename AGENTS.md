# CS 학습자료 작업 규칙

## 목적과 시작 순서

한국어 CS 학습자료와 기술 면접 대비 자료를 관리한다. 새 작업에서는 README.md, progress.md, sources/README.md를 먼저 읽고 해당 주제 문서를 확인한다. 자료 제작 완료와 학습자 숙련도를 구분한다.

## 작성 기준

- Notion 원본의 공통 논점을 종합하되 문장을 대량 복사하지 않는다.
- 원본 주소와 수정일 등 비공개 메타데이터는 Git에서 제외되는 로컬 출처 기록에만 보관한다. 공개 문서에는 공개 참고문서와 작성 기준일을 기록한다. 접근 불가, 잘린 응답, 미지원 블록을 읽은 것처럼 취급하지 않는다.
- 정의 → 필요성 → 원리 → 예시 → 비교·예외 → 면접 질문 순서로 설명한다.
- 프로토콜/프레임워크 버전과 가정을 적는다. 불확실하거나 변할 수 있는 세부사항은 공식 문서로 확인한다.
- 원본에서 가져온 주제와 새로 보완한 설명·연습을 구분한다.
- 중요한 수정은 sources/corrections.md에 근거와 함께 기록한다.
- 그림은 가능한 경우 Markdown 표나 Mermaid로 작성한다.

## 공개 자료 범위

이 저장소는 공개다. 개인 이름·연락처, 인증 정보, 비공개 Notion 링크·페이지 ID·수정일 목록, 원본 덤프, 서명된 첨부 URL, 권리가 확인되지 않은 이미지/PDF 원본은 커밋하지 않는다. Notion 원본은 그대로 유지한다. 자동 동기화가 있다고 주장하지 않는다.

## 학습 진행

- 모의면접은 한 번에 질문 하나를 제시하고 사용자의 답변을 기다린다.
- 답변 전 모범 답안을 먼저 노출하지 않는다.
- 채점은 실제 답변에 근거한다. 사용자가 답하지 않은 내용으로 점수나 완료 상태를 만들지 않는다.
- 진행 기록에는 주제·점수·오해·다음 복습을 간단히 남긴다. 개인적인 대화 원문을 공개 저장소에 넣지 않는다.

## Git과 검토

- 변경 전에 git status와 현재 브랜치를 확인한다. 사용자의 미커밋 변경을 덮어쓰지 않는다.
- 다른 컴퓨터에서 이어갈 때는 먼저 원격 변경을 확인한다. 충돌을 force push나 hard reset으로 숨기지 않는다.
- 관련 Markdown 링크가 실제 파일로 연결되는지 확인한다.
- 질문 개수·목차·자료 상태가 실제 내용과 맞는지 확인한다.
- 게시 전 변경 파일 목록과 민감 정보·임시 파일 포함 여부를 검토한다.
- 게시 후 원격 커밋과 파일을 확인하고 실제 완료 범위와 미완료 범위를 보고한다.
